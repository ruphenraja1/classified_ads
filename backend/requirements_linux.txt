# Minimal Linux production requirements for the Django backend
# Use Python 3.10+ on the Linux server.

# Core
Django==4.2
djangorestframework==3.14.0
django-cors-headers==4.9.0
whitenoise==6.12.0
python-decouple==3.8
python-dotenv==1.0.0
dj-database-url==3.1.2

# Database
psycopg2-binary==2.9.11

# Server
gunicorn==25.1.0

# Environment
# No Windows-only dependencies here.
# Add any extra runtime packages only if your production app actually imports them.
